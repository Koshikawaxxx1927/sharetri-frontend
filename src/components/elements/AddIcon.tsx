import AddCircleIcon from "@mui/icons-material/AddCircle";
import { red } from "@mui/material/colors";

const AddIcon = () => {
  return (
    <AddCircleIcon
      sx={{ color: red[500], fontSize: "60px" }}
    />
  );
};

export default AddIcon;
